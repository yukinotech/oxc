# Oxlint 结构图

```mermaid
graph LR
  subgraph Rust Crates
    OLC[[crates/oxc_linter\nLint 引擎]]
  end

  subgraph "apps/oxlint (Rust)"
    CLI[CLI / Config / Walk\nsrc/command+lint+walk]
    RUN[N-API 入口\nsrc/run.rs]
    JSBridge[JS 插件桥接\nsrc/js_plugins/*]
  end

  subgraph "apps/oxlint (TS)"
    CLIJS[CLI 启动器\nsrc-js/cli.ts]
    BindJS[napi bindings\nsrc-js/bindings.*]
    PluginsJS[插件运行时\nsrc-js/plugins/*]
  end

  subgraph 用户代码
    PluginAuthor[JS 插件作者]
  end

  CLI -->|LintCommand & Walk| OLC
  RUN -->|调用 CliRunner| OLC
  RUN <-->|ThreadsafeFunction| JSBridge
  JSBridge <-->|ExternalLinter| OLC
  CLIJS -->|process.argv + 回调| BindJS --> RUN
  RUN -->|lintFile/loadPlugin| PluginsJS
  PluginsJS -->|提供 Context/Visitor| PluginAuthor
  PluginAuthor -->|definePlugin/defineRule| PluginsJS
```

## 说明
- **crates/oxc_linter**：核心 lint 引擎，负责解析配置、运行规则、合成诊断。
- **apps/oxlint/src**：Rust 应用层，提供 CLI、N-API 绑定及 JS 插件桥接逻辑，最终驱动 `oxc_linter::Linter`。
- **apps/oxlint/src-js**：Node 侧运行时，实现 CLI 入口、napi 绑定加载、JS 插件上下文与 AST 访问。
- **用户插件**：通过 `definePlugin/defineRule` API 接入，被 Oxlint 在运行期加载并执行。
